spring:
  application:
    name: history-service
  datasource:
    url: jdbc:h2:file:./data/historydb
    driver-class-name: org.h2.Driver
    username: suyash
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true

server:
  port: 8083